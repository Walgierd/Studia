/*
 * Controller package for the Counter-Strike match results application.
 * It acts as a bridge between the model (data) and the view (UI),
 * managing user input and coordinating program flow.
 */
package com.mycompany.lab1.controller;

/**
 * Controller package contains classes that coordinate actions between the
 * model and the view, and handle user input logic.
 *
 * <p>This package is part of the MVC (Model–View–Controller) architecture
 * of the application.</p>
 *
 * @author Olivier
 * @version 1.0
 */
public class PackageInfo {
}
